<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kogile.memberboard.Mapper.MemberBoardMapper">
	<cache />
	<select id="memberBoard" parameterType="MemberSearch" resultType="MemberBoard">
		select exter_m_no as no, exter_mem_name as name, exter_m_email as mail, interlinked_info_num as infoNum
		from external_m_info
			<where>
					exter_m_email LIKE #{search}
			</where>
	</select>
</mapper>